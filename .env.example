# ============================================
# AgentBridge — Environment Variables
# ============================================
# Copy this file to .env.local (in apps/web/) and fill in your values.
# See apps/web/.env.example for full documentation.

# ---- Database ----
# DATABASE_PATH=/app/apps/web/data/agentbridge.db

# ---- Supabase Auth (optional — omit to disable auth) ----
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# ---- Credential Vault (recommended for hosted secure calling) ----
# 32+ chars, used to encrypt API credentials and OAuth tokens at rest.
# AGENTBRIDGE_ENCRYPTION_KEY=replace-with-a-long-random-secret

# ---- Server ----
# NODE_ENV=production
# PORT=3000
