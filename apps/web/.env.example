# ============================================
# AgentBridge Web — Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values.
# Only NEXT_PUBLIC_* variables are exposed to the browser.

# ---- Database ----
# Path to the SQLite database file. Defaults to ./agentbridge.db
# DATABASE_PATH=/app/apps/web/data/agentbridge.db

# ---- Supabase Auth (optional) ----
# If these are not set, auth is disabled — all features work without login.
# Set both to enable user accounts, API ownership, and per-user dashboards.
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# ---- Credential Vault (required for secure credential storage + OAuth tokens) ----
# Use a long random secret (32+ chars). Keep this private.
# AGENTBRIDGE_ENCRYPTION_KEY=replace-with-a-long-random-secret

# ---- Server ----
# NODE_ENV=production
# PORT=3000
