{
  "name": "agentbridge",
  "version": "0.1.0",
  "private": true,
  "description": "Make any app agent-ready \u2014 interact with any app using natural language",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "sharp"
    ]
  }
}